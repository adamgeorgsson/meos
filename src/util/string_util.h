#pragma once

#include <string>

namespace meos::util {

// These functions are now provided in meos_util.h (returning const& from thread-local buffers).
// This header is retained for any callers that need the meos::util namespace include.

}  // namespace meos::util
